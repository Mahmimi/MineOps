# Platform Kubernetes

This directory is reserved for Kubernetes platform resources.

V1 prepares the structure only. It does not define Minecraft, Playit, Discord bot, or controller workloads yet.

Planned areas:

- `minecraft/` for future 0-or-1 Minecraft server workload resources
- `playit/` for future playit tunnel resources
- `storage/` for future persistent volume and backup resources
